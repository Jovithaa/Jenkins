pipeline {
  agent any
  stages {
    stage('version') {
      steps {
	echo 'Stage Version'
        
      }
    }
    stage('hello') {
      steps {
	echo 'Stage Hello'
        bat 'docker run -d -p 5000:5000 flaskwebapp'
      }
    }
  }
}
